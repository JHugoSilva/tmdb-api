services:
  app:
    build:
      context: .
      dockerfile: docker/php/Dockerfile
    container_name: lwsa-app
    working_dir: /var/www
    volumes:
      - ./backend:/var/www
    networks:
      - lwsa_net

  mysql:
    image: mysql:8
    container_name: mysql_lwsa
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: lwsa_db
    ports:
      - "3308:3306"
    volumes:
      - dbdata:/var/lib/mysql
    networks:
      - lwsa_net

  nginx:
    image: nginx:stable-alpine
    container_name: nginx_lwsa
    ports:
      - "8088:80"
    volumes:
      - ./backend:/var/www
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - app
    networks:
      - lwsa_net

  phpmyadmin:
    image: phpmyadmin/phpmyadmin
    container_name: phpmyadmin_lwsa
    environment:
      PMA_HOST: mysql
      MYSQL_ROOT_PASSWORD: root
    ports:
      - "8081:80"
    depends_on:
      - mysql
    networks:
      - lwsa_net

  frontend:
    build: 
      context: .
      dockerfile: docker/vue/Dockerfile
    container_name: vue_lwsa
    ports:
      - "5177:5173"
    networks:
      - lwsa_net

volumes:
  dbdata:

networks:
  lwsa_net:
    driver: bridge

